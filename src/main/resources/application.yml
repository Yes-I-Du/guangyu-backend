server:
  port: 8158
  servlet:
    context-path: /api

spring:
  application:
    name: guangyu-backend
  profiles:
    active: local
  #数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://117.72.87.42:3306/guangyu_picture?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: Root@1234

mybatis-plus:
  configuration:
    # MyBatis 配置
    map-underscore-to-camel-case: false
    #仅在开发环境打印日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
      db-config:
        logic-delete-field: isDelete #全局逻辑删除字段名
        logic-delete-value: 1 #逻辑已删除值
        logic-not-delete-value: 0 #逻辑未删除值
#接口文档配置
knife4j:
  enable: true
  openapi:
    title: guangyu接口文档
    version: v1.0
    group:
      default:
        api-rule: package
        api-rule-resources:
          - com.guangyu.guangyubackend.controller